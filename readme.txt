api para crud producto alojada en el puerto 4000
como base de datos se uso json fake server la cual se tiene que inicializar
con este comando json-server db.json --port 3000 en el puerto 3000
ya que asi lo deje estipulado al momento de realizar el axios para el crud
